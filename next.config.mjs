/**
 * @type {import('next').NextConfig}
 */

const nextConfig = {
  images: {
    loader: "akamai",
    path: "",
  },
  // assetPrefix: "./",
  // output: 'export',
  // trailingSlash: true,


};

export default nextConfig;
